/**
 * url
 * name
 * 思路：
 **/


#include <iostream>

using namespace std;

#define MAXN 100

int main() {

    ios::sync_with_stdio( false );
    cin.tie( 0 );

    int n, m, ans = 0;

    cin >> n;

    for( int i = 0; i < n; ++i ) {
        cin >> a[i];
    }

    cout << ans;

    return 0;
}